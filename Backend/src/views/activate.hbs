<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Activation</title>
</head>
<style>
  .container {
    display: flex;
    justify-content: center;
    margin-top: 10rem;
    flex-flow: row;
  }

  .title-bg-image {
    margin-right: 5rem;
  }

  .title-bg-image img {
    width: 20rem;
  }

  .state-message {
    justify-self: center;
    align-self: center;
  }

  .state {
    display: flex;
    justify-content: center;
    column-gap: 1rem;
  }

  .state img {
    width: 4rem;
  }
</style>
<body>
<div class="container">
  <div class="title-bg-image">
    <h1>Account Activation</h1>
    <div class="bg-image">
      <img src="/images/activate-account.png" alt="">
    </div>
  </div>
  <div class="state-message">
    <div class="state">
      {{#if state}}
        <img src="/images/successful.png" alt="" id="state-icon-successfully" style="{{#if (eq state 'Successfully')}}display: block;{{else}}display: none;{{/if}}">
        <img src="/images/fail.png" alt="" id="state-icon-fail" style="{{#if (eq state 'Error')}}display: block;{{else}}display: none;{{/if}}">
        <h3 id="state">{{ state }}</h3>
      {{/if}}
    </div>
    <div class="message">
      <h4 id="message">{{ message }}</h4>
    </div>
  </div>
</div>
</body>
</html>